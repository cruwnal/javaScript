$(document).ready(() => {
$('.product-photo').show()
});
___________

$(document).ready(() => {
$('#nav-dropdown').hide()
});
________________

$(document).ready(() => {
  let $navDropdown = $('#nav-dropdown');
  $navDropdown.hide()
});
